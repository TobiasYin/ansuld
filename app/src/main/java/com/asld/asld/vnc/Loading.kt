package com.asld.asld.vnc

import android.content.Context
import android.view.View
import android.widget.ProgressBar

class Loading(context: Context, val text: String) : View(context) {
    init {

    }
}